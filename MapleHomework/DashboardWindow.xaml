<ui:FluentWindow x:Class="MapleHomework.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:MapleHomework"
        xmlns:converters="clr-namespace:MapleHomework.Converters"
        Title="전체 캐릭터 현황" 
        Height="950" Width="1350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackground}"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        UseLayoutRounding="True">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:ProgressToWidthConverter x:Key="ProgressToWidthConverter"/>
        <converters:TasksToTooltipConverter x:Key="TasksToTooltipConverter"/>
        
        <!-- 진행률 바 색상 -->
        <LinearGradientBrush x:Key="DailyGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#5AC8FA" Offset="0"/>
            <GradientStop Color="#4ECDC4" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="WeeklyGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#FF9500" Offset="0"/>
            <GradientStop Color="#FFCC00" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="BossGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#F97316" Offset="0"/>
            <GradientStop Color="#FB923C" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 모던 타이틀 바 -->
        <Border Grid.Row="0" Background="{DynamicResource CardBackground}" 
                BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1"
                Padding="24,16" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#155AC8FA" CornerRadius="12" Padding="12" Margin="0,0,16,0">
                        <ui:SymbolIcon Symbol="DataBarVertical24" FontSize="24" Foreground="#5AC8FA"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="전체 캐릭터 현황" Foreground="{DynamicResource TextPrimary}" 
                                   FontSize="20" FontWeight="Bold"/>
                        <TextBlock Text="{Binding CharacterCount, StringFormat='총 {0}개 캐릭터 관리 중'}" 
                                   Foreground="{DynamicResource TextMuted}" FontSize="12" Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Border Background="{DynamicResource CardBackgroundAlt}" CornerRadius="8" Padding="12,8" Margin="0,0,16,0"
                            Visibility="{Binding HasPendingTasks, Converter={StaticResource BoolToVis}}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="AlertOn24" FontSize="16" Foreground="#F97316" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding TotalPendingCount}" Foreground="#F97316" FontWeight="Bold" FontSize="14"/>
                            <TextBlock Text="개 남음" Foreground="{DynamicResource TextMuted}" FontSize="12" Margin="4,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <Button Click="MinimizeButton_Click" Width="40" Height="40" Padding="0"
                            Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="최소화">
                        <ui:SymbolIcon Symbol="Subtract24" FontSize="18" Foreground="{DynamicResource TextMuted}"/>
                    </Button>
                    <Button Click="CloseButton_Click" Width="40" Height="40" Padding="0"
                            Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="닫기">
                        <ui:SymbolIcon Symbol="Dismiss24" FontSize="18" Foreground="{DynamicResource TextMuted}"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 메인 컨텐츠 -->
        <Grid Grid.Row="1" Margin="20,16,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="550"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <!-- 왼쪽: 오늘의 할 일 -->
            <Border Grid.Column="0" Background="{DynamicResource CardBackground}" CornerRadius="16" Margin="0,0,16,0"
                    BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.08"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 헤더 -->
                    <Border Grid.Row="0" CornerRadius="16,16,0,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#FF9500" Offset="0"/>
                                <GradientStop Color="#FFCC00" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Grid Margin="20,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <Border Background="#20FFFFFF" CornerRadius="8" Padding="8" Margin="0,0,14,0">
                                    <ui:SymbolIcon Symbol="Star24" FontSize="20" Foreground="White"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="오늘의 할 일" Foreground="White" FontSize="17" FontWeight="Bold"/>
                                    <TextBlock Text="즐겨찾기 기반 숙제 현황" Foreground="#C0FFFFFF" FontSize="11" Margin="0,2,0,0"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="즐겨찾기만" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.9"/>
                                <ui:ToggleSwitch IsChecked="{Binding ShowOnlyFavorites, Mode=TwoWay}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- 할 일 목록 -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="16">
                        <StackPanel>
                            <!-- 할 일이 있을 때 -->
                            <ItemsControl ItemsSource="{Binding GroupedTasks}" Visibility="{Binding HasPendingTasks, Converter={StaticResource BoolToVis}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource CardBackgroundAlt}" CornerRadius="14" Padding="20" Margin="0,0,0,14"
                                                BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                                            <StackPanel>
                                                <!-- 캐릭터 헤더 -->
                                                <Grid Margin="0,0,0,14">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <Border Width="56" Height="56" CornerRadius="28" ClipToBounds="True"
                                                            BorderBrush="{DynamicResource AccentCyan}" BorderThickness="2">
                                                        <Border.Background>
                                                            <ImageBrush Stretch="UniformToFill"
                                                                ImageSource="{Binding ImageUrl, FallbackValue={x:Null}, TargetNullValue={x:Null}}">
                                                                <ImageBrush.RelativeTransform>
                                                                    <ScaleTransform CenterX="0.5" CenterY="0.55" ScaleX="3.5" ScaleY="3.5"/>
                                                                </ImageBrush.RelativeTransform>
                                                            </ImageBrush>
                                                        </Border.Background>
                                                    </Border>
                                                    
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="14,0,0,0">
                                                        <TextBlock Text="{Binding Nickname}" Foreground="{DynamicResource TextPrimary}" 
                                                                   FontSize="16" FontWeight="Bold"/>
                                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                            <TextBlock Text="Lv." Foreground="{DynamicResource AccentCyan}" FontSize="12" FontWeight="Bold"/>
                                                            <TextBlock Text="{Binding Level}" Foreground="{DynamicResource TextSecondary}" 
                                                                       FontSize="13" FontWeight="SemiBold" Margin="2,0,0,0"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                    
                                                    <Border Grid.Column="2" CornerRadius="10" Padding="14,8" VerticalAlignment="Center">
                                                        <Border.Background>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                                <GradientStop Color="#F97316" Offset="0"/>
                                                                <GradientStop Color="#FB923C" Offset="1"/>
                                                            </LinearGradientBrush>
                                                        </Border.Background>
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="{Binding PendingCount}" Foreground="White" FontSize="16" FontWeight="Bold"/>
                                                            <TextBlock Text="개 남음" Foreground="#FFCCCC" FontSize="11" VerticalAlignment="Bottom" Margin="4,0,0,1"/>
                                                        </StackPanel>
                                                    </Border>
                                                </Grid>

                                                <!-- 구분선 -->
                                                <Border Height="1" Background="{DynamicResource DividerColor}" Margin="0,0,0,14"/>

                                                <!-- 숙제 목록 -->
                                                <ItemsControl ItemsSource="{Binding Tasks}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <UniformGrid Columns="{Binding DataContext.TaskColumnCount, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border CornerRadius="10" Padding="12,10" Margin="0,0,8,8"
                                                                    Background="{Binding CategoryColor}">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <Border Background="#30FFFFFF" CornerRadius="4" Padding="6,3" VerticalAlignment="Center">
                                                                        <TextBlock Text="{Binding CategoryText}" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                                                    </Border>
                                                                    <TextBlock Text="{Binding TaskName}" Foreground="White" FontSize="12" FontWeight="Medium" 
                                                                               VerticalAlignment="Center" Margin="8,0,0,0" TextTrimming="CharacterEllipsis"/>
                                                                </StackPanel>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- 할 일이 없을 때 -->
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,80,0,0">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasPendingTasks}" Value="False">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <Border Background="#1510B981" CornerRadius="40" Width="80" Height="80" Margin="0,0,0,20">
                                    <ui:SymbolIcon Symbol="CheckmarkCircle24" FontSize="40" Foreground="#10B981"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="{Binding EmptyMessage}" Foreground="{DynamicResource TextSecondary}" FontSize="17" FontWeight="SemiBold"
                                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                                <TextBlock Text="{Binding EmptySubMessage}" Foreground="{DynamicResource TextMuted}" FontSize="13" 
                                           HorizontalAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap" TextAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- 오른쪽: 캐릭터 카드 목록 -->
            <Border Grid.Column="1" Background="{DynamicResource CardBackground}" CornerRadius="16"
                    BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.08"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 헤더 -->
                    <Border Grid.Row="0" CornerRadius="16,16,0,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#5AC8FA" Offset="0"/>
                                <GradientStop Color="#818CF8" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Grid Margin="20,14">
                        <StackPanel Orientation="Horizontal">
                                <Border Background="#20FFFFFF" CornerRadius="8" Padding="8" Margin="0,0,12,0">
                                    <ui:SymbolIcon Symbol="People24" FontSize="18" Foreground="White"/>
                                </Border>
                                <TextBlock Text="캐릭터 목록" Foreground="White" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                        </Grid>
                    </Border>

                    <!-- 캐릭터 리스트 -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="14">
                        <ItemsControl ItemsSource="{Binding Characters}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource CardBackgroundAlt}" CornerRadius="14" Padding="16" Margin="0,0,0,12" Cursor="Hand"
                                            BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftClick" 
                                                          Command="{Binding DataContext.SelectAndCloseCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                          CommandParameter="{Binding}"/>
                                        </Border.InputBindings>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource CardBackgroundHover}"/>
                                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentCyan}"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- 캐릭터 이미지 -->
                                            <Border Width="70" Height="70" CornerRadius="35" ClipToBounds="True" VerticalAlignment="Center"
                                                    BorderBrush="{DynamicResource AccentCyan}" BorderThickness="2">
                                                <Border.Background>
                                                    <ImageBrush Stretch="UniformToFill"
                                                                ImageSource="{Binding ImageUrl, TargetNullValue={x:Null}, FallbackValue={x:Null}}">
                                                        <ImageBrush.RelativeTransform>
                                                            <ScaleTransform CenterX="0.5" CenterY="0.55" ScaleX="3.5" ScaleY="3.5"/>
                                                        </ImageBrush.RelativeTransform>
                                                    </ImageBrush>
                                                </Border.Background>
                                            </Border>

                                            <!-- 캐릭터 정보 -->
                                            <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Nickname}" Foreground="{DynamicResource TextPrimary}" 
                                                           FontSize="15" FontWeight="Bold"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <TextBlock Text="Lv." Foreground="{DynamicResource AccentCyan}" FontSize="11" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding Level}" Foreground="{DynamicResource TextPrimary}" 
                                                               FontSize="12" FontWeight="SemiBold" Margin="2,0,8,0"/>
                                                    <TextBlock Text="{Binding CharacterClass}" Foreground="{DynamicResource TextMuted}" 
                                                               FontSize="11" TextTrimming="CharacterEllipsis"/>
                                                </StackPanel>
                                                
                                                <!-- 진행률 바 -->
                                                <Grid Margin="0,12,0,0">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    
                                                    <!-- 일일 -->
                                                    <Grid Grid.Row="0" Margin="0,0,0,6">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="32"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="36"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="일일" Foreground="#5AC8FA" FontSize="10" FontWeight="Bold"/>
                                                        <Border Grid.Column="1" Height="6" CornerRadius="3" Background="{DynamicResource DividerColor}">
                                                            <Border CornerRadius="3" Background="{StaticResource DailyGradient}" HorizontalAlignment="Left"
                                                                    Width="{Binding DailyProgress, Converter={StaticResource ProgressToWidthConverter}}"/>
                                                        </Border>
                                                        <TextBlock Grid.Column="2" Text="{Binding DailyProgress, StringFormat='{}{0:F0}%'}" 
                                                                   Foreground="#5AC8FA" FontSize="10" FontWeight="Bold" HorizontalAlignment="Right"/>
                                                    </Grid>
                                                    
                                                    <!-- 주간 -->
                                                    <Grid Grid.Row="1" Margin="0,0,0,6">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="32"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="36"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="주간" Foreground="#FF9500" FontSize="10" FontWeight="Bold"/>
                                                        <Border Grid.Column="1" Height="6" CornerRadius="3" Background="{DynamicResource DividerColor}">
                                                            <Border CornerRadius="3" Background="{StaticResource WeeklyGradient}" HorizontalAlignment="Left"
                                                                    Width="{Binding WeeklyProgress, Converter={StaticResource ProgressToWidthConverter}}"/>
                                                        </Border>
                                                        <TextBlock Grid.Column="2" Text="{Binding WeeklyProgress, StringFormat='{}{0:F0}%'}" 
                                                                   Foreground="#FF9500" FontSize="10" FontWeight="Bold" HorizontalAlignment="Right"/>
                                                    </Grid>
                                                    
                                                    <!-- 보스 -->
                                                    <Grid Grid.Row="2">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="32"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="36"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="보스" Foreground="#F97316" FontSize="10" FontWeight="Bold"/>
                                                        <Border Grid.Column="1" Height="6" CornerRadius="3" Background="{DynamicResource DividerColor}">
                                                            <Border CornerRadius="3" Background="{StaticResource BossGradient}" HorizontalAlignment="Left"
                                                                    Width="{Binding BossProgress, Converter={StaticResource ProgressToWidthConverter}}"/>
                                                        </Border>
                                                        <TextBlock Grid.Column="2" Text="{Binding BossProgress, StringFormat='{}{0:F0}%'}" 
                                                                   Foreground="#F97316" FontSize="10" FontWeight="Bold" HorizontalAlignment="Right"/>
                                                    </Grid>
                                                </Grid>
                                                
                                                <!-- 수익 정보 -->
                                                <Border CornerRadius="8" Padding="10,8" Margin="0,12,0,0">
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#15FFD700" Offset="0"/>
                                                            <GradientStop Color="#10FFA500" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                    <Grid>
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="💰" FontSize="12" Margin="0,0,6,0"/>
                                                            <TextBlock Text="{Binding WeeklyBossRewardText}" Foreground="#FFD700" FontSize="12" FontWeight="Bold"/>
                                                        </StackPanel>
                                                        <TextBlock Text="{Binding WeeklyBossExpectedRewardText, StringFormat='/ {0}'}" 
                                                                   Foreground="{DynamicResource TextMuted}" FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                                    </Grid>
                                                </Border>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
